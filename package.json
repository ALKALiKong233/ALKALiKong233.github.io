{
  "name": "blog",
  "private": true,
  "scripts": {
    "build": "hugo --minify",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "hugo-bin": "^0.150.0",
    "wrangler": "^4.19.1"
  }
}

